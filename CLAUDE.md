# Dr. Victor Ferigato Conteúdo Studio (memória persistente do projeto)

Este repositório é o **Dr. Victor Ferigato Conteúdo Studio**: edição e agendamento de conteúdo para as redes da **Dr. Victor Ferigato**. Estúdio da agência com infraestrutura compartilhada; o posicionamento é o desta marca.

**Antes de editar qualquer vídeo ou escrever qualquer caption, leia `FRAMEWORK.md`** (posicionamento, regras inegociáveis, formatos, assinaturas de edição e todos os gotchas técnicos).

## Regras que valem em qualquer resposta pública

- Nunca usar travessão em texto público (caption, lettering, legenda): reescrever a frase.
- Quando citar a criadora ou criador: sempre a credencial completa **"PENDENTE (credencial completa de quem cria, para citação em texto público)"**.
- Palavrão em vídeo **se bipa, não se corta**.
- Loudness final: **-14 LUFS**.

## Working dirs

- Estúdio: este repo (symlink `~/drvictorferigato-conteudo` aponta para cá). Projetos em `projects/<nome>/`.
- Ferramentas: `video-use` e `hyperframes` clonados em `/workspace/browser-use/` e `/workspace/heygen-com/` (Linux/cloud) ou `~/video-editor/` (Mac). Skills registradas em `~/.claude/skills/`.
- **Remotion**: composições versionadas em `remotion/` (React/TS); `node_modules` fora do git, instalado pelo `setup.sh`. `npm run studio` abre o editor, `npx remotion render src/index.ts <Composicao> saida.mp4` renderiza. Componentes prontos: `Aurora` (fundo) e `CartaoTitulo`. A paleta vive **só** em `remotion/src/marca.ts`, hoje com valores **PLACEHOLDER** (ver "Pistas de marca"). Regra de escolha HyperFrames vs Remotion: `FRAMEWORK.md`.
- **Scripts genéricos** em `scripts/` (portados do `profissioai-conteudo` em 18/set/2026, zero referência de marca, conferido por varredura): `decupar.py` (decupagem por âncora de texto), `relatorio_decupagem.py` (relatório do que ficou e do que caiu), `gera_lut_slog2.py` (LUT S-Log2/S-Gamut para Rec.709), `zip_index_remoto.py` (lista e extrai de ZIP gigante no Drive por range request), `gera_imagem.py` (OpenAI ou Gemini, chaves só do ambiente), `sobe_para_drive.py` (upload com token). Dependem de `ffmpeg`/`ffprobe`, `python3` com `numpy`, `pillow` e `colour-science`, e `curl`; o `setup.sh` instala tudo.
- Ambiente novo (container limpo): `bash /home/user/drvictorferigato-conteudo/scripts/setup.sh` e depois `bash scripts/validate.sh`. **No campo de setup do environment, caminho absoluto** (ver gotcha do caminho relativo). Em sessão nova, conferir `ls /workspace` antes de contar com video-use ou hyperframes; se estiver vazio, rodar o setup à mão.

## IDs e contas (verificados em 18/set/2026)

- Instagram da marca: **@drvictorferigato**.
- **Metricool**: conta da agência **suporte@zavi.ag**. Marca **"drvictorferigato"**, **blog_id 6606429**, timezone America/Sao_Paulo (`getBrandSettings` em 18/set/2026). **Só o Instagram está conectado** (`networksData: {instagramData: "drvictorferigato"}`): sem Facebook, TikTok, YouTube, LinkedIn nem Pinterest. Enquanto for assim, a regra "todos os canais conectados" resulta em post só de Instagram; conectar as demais redes no painel se o formato for previsto. Melhor horário de publicação: medir com `getBestTimeToPostByNetwork`.
- **Regra de agendamento (todas as marcas da agência)**: sempre incluir TODOS os canais conectados da marca no post, exceto YouTube horizontal. YouTube entra como **Short** (`youtubeData: {type: "short", title, madeForKids: false}`); Instagram como REEL; Facebook como REEL; TikTok, LinkedIn e Pinterest com networkData padrão. Nunca publicar vídeo vertical como YouTube horizontal comum.
- **Kairogen**: conta da agência **suporte@zavi.ag**, **plano FREE, 0 créditos, 1 geração concorrente** (`get_me_context` em 18/set/2026). **B-roll por IA indisponível até haver créditos.**
- **ElevenLabs**: chave `sk_` (51 chars) na env var `ELEVENLABS_API_KEY` do environment (presente em 18/set/2026); o setup grava em `.env` na raiz do video-use. Escopos necessários: TTS, STT Scribe, sound-generation, voices_read. Plano e cota: **PENDENTE conferir** (testar escopo sem gastar crédito: ver gotcha). Voz da marca para narração: **PENDENTE (voice_id, modelo e settings)**.
- **OpenAI (imagem)**: `OPENAI_API_KEY` **ausente** no environment e `api.openai.com` **bloqueado** na allowlist (medido em 18/set/2026). `scripts/gera_imagem.py openai` não funciona até cadastrar a chave no environment, liberar o host e abrir sessão nova. **PENDENTE.**
- **Gemini (imagem)**: `GEMINI_API_KEY` **ausente** e `generativelanguage.googleapis.com` **bloqueado**. Mesma pendência. Ao cadastrar, conferir que o projeto da chave está no faturamento: se o erro citar `quotaId: ...-FreeTier` com `limit: 0`, o vínculo não pegou (ler o campo `details`, não só a mensagem).
- **Google Drive**: conector oficial conectado (conta da agência). A pasta da marca no Drive se chama **"Dr. Victor Ferigato"** e os brutos ficam na subpasta **`Captacoes`** (localizar pelo conector, por nome; há um bruto de podcast de mais de 11 GB, caso típico para download por range). Para download direto por curl a pasta ou o arquivo precisa estar em "qualquer pessoa com o link: leitor": **PENDENTE conferir/tornar público** antes do primeiro projeto. **Não gravar IDs de pastas privadas nem nomes de arquivos de cliente aqui: o repo é público.**
- **Segurança**: documentos da agência no Drive podem conter dados de acesso do cliente. **Nunca copiar login, senha ou token para este repo** (é público) nem para o chat; se um resultado de busca trouxer isso, ignorar e não citar.

## Pistas de marca encontradas no Drive (18/set/2026, a confirmar em briefing)

São indícios lidos nos briefings da agência, **não são regra** até o cliente confirmar. Persona, tom, CTA padrão, credencial e paleta só entram no `FRAMEWORK.md` depois disso.

- **Credencial**, em variações que já circularam publicamente na bio e nos posts: "Médico Nutrólogo | Fisiculturista", "Radio-oncologista | Nutrólogo | Campeão de Fisiculturismo", "Médico do esporte". A forma oficial para citação pública segue **PENDENTE**: escolher uma com o cliente.
- **Produto recorrente**: **Spotter**, assistente de dieta e treino no WhatsApp criado pelo Dr. Victor, divulgado abertamente no perfil. É o CTA de quase todo carrossel de 2026.
- **Identidade visual**: os títulos do feed usam **laranja** como acento, com orientação recente da equipe para usá-lo com mais parcimônia. Fontes citadas nos briefings: **Akkordeon** e **Poppins**. Hex e proporções: **PENDENTE**.
- **Fontes de conteúdo** (todas no Drive da agência, localizar pelo conector): briefings mensais de carrossel e estático, o roteiro de perguntas para gravação de reels, a proposta de conteúdo 2026 (dicas semanais, 3 assuntos por semana, Dr. Victor como filtro do que funciona e do que é só marketing) e o briefing de cortes do podcast.

## Rede do environment (medido em 18/set/2026)

Network **Custom**. Diagnóstico em um comando: `curl -sv https://host/ 2>&1 | grep CONNECT`. `HTTP/1.1 403` no CONNECT é allowlist; qualquer outra resposta significa que a rede passou e o problema é outro (chave, quota, rota). A allowlist é **literal por subdomínio**: para um site inteiro, `*.dominio.com` junto do apex.

| Host | Status | Para quê |
|---|---|---|
| `api.elevenlabs.io` | OK | TTS, STT, SFX |
| `drive.google.com`, `drive.usercontent.google.com` | OK | baixar brutos |
| `pypi.org`, `files.pythonhosted.org`, `registry.npmjs.org` | OK **pelo proxy** | vêm em `no_proxy` e batem direto no firewall (403); o `setup.sh` reroteia pelo agent proxy |
| `github.com`, `api.github.com`, `objects.githubusercontent.com` | OK | clones e ffmpeg estático |
| `raw.githubusercontent.com` | 403 | **não precisa liberar**: skills do hyperframes vêm do clone |
| `api.openai.com` | **403** | `gera_imagem.py openai`. **PENDENTE liberar** |
| `generativelanguage.googleapis.com` | **403** | `gera_imagem.py gemini`. **PENDENTE liberar** (`www.googleapis.com` não cobre) |
| `www.googleapis.com` | **403** | `sobe_para_drive.py`. **PENDENTE liberar** se for subir arquivo para o Drive |
| `archive.ubuntu.com` | 403 | `apt` indisponível; ffmpeg vem estático do GitHub Releases |

Variáveis de ambiente em 18/set/2026: `ELEVENLABS_API_KEY` presente; `OPENAI_API_KEY` e `GEMINI_API_KEY` **ausentes**. Variável entra na criação do container: cadastrar no environment e **abrir sessão nova**; conferir com `printenv | grep -c API_KEY` antes de acusar o script.

## Gotchas essenciais (herdados dos estúdios da agência, todos validados)

- Brutos de iPhone são HLG 10-bit: gerar proxy SDR uma vez antes de editar (filtro `colorspace=all=bt709:itrc=bt2020-10:iprimaries=bt2020:ispace=bt2020nc`).
- **Brutos de Sony em S-Log2 (A7 III): converter, não "filtrar".** O XML lateral de cada clipe (`C00xxM01.XML`) declara `CaptureGammaEquation` e `CaptureColorPrimaries`; quando diz `s-log2`/`s-gamut`, a imagem chega chapada e precisa de conversão para Rec.709. `scripts/gera_lut_slog2.py` gera a LUT com a `colour-science` (log para linear, primárias, ombro, gama). Dois cuidados que a prática impôs: **exposição -0,5 stop e joelho em 0,65**, senão o branco estoura; e conferir que o ffmpeg aplica a `lut3d` **em RGB, não em YUV** (ele auto-insere `yuvj420p` para `rgb24`; verificar com `-v verbose`). Saída sempre com `out_range=tv` e `-color_range tv`, senão o arquivo sai `yuvj420p` e destoa entre players.
- **Câmera pode gravar na vertical sem gravar a flag de rotação.** O arquivo vem 3840x2160 deitado e o ffprobe não mostra rotação nenhuma; só olhando um frame se descobre. Corrigir com `transpose=1` (90° horário) antes de escalar. Checar um frame de qualquer lote novo antes de planejar o corte.
- **Decupagem por âncora de texto, não por timecode.** `scripts/decupar.py` recebe um `edl.json` onde cada trecho é "de tal frase até tal frase"; ele casa as âncoras contra a transcrição com timestamp por palavra do Scribe e resolve os tempos. Revisar um corte vira editar uma frase. O campo `apos` empurra o cursor quando a mesma frase aparece antes, dita pela equipe fora de cena.
- **Upload de vídeo para o Drive não é possível deste container** enquanto `www.googleapis.com` estiver fora da allowlist (403 no CONNECT, medido em 18/set/2026). O conector MCP do Drive só aceita conteúdo via `base64Content` dentro da própria chamada, inviável para vídeo. **Criar pasta funciona** (não tem conteúdo). Entrega de vídeo sai por commit na branch ou pelo envio direto do arquivo na conversa; `scripts/sobe_para_drive.py` só passa a servir depois de liberar o host.
- **Processo em background com `nohup`/`setsid` é recolhido quando a tool call retorna.** Ingest e render já morreram no meio assim, e uma vez dois loops escreveram o mesmo arquivo ao mesmo tempo e o corromperam. Usar `run_in_background: true` da própria ferramenta Bash, que o harness rastreia, ou deixar estourar o timeout do primeiro plano. Em lote longo, `flock` num arquivo de lock evita a corrida.
- **Remotion renderiza com o `headless_shell`, não com o Chromium do Playwright.** O `chromium-1194` removeu o headless antigo que o Remotion pede e o launch morre com "Old Headless mode has been removed". O binário certo é `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`, fixado em `remotion/remotion.config.ts`. Baixar o browser próprio do Remotion não é opção: está fora da allowlist.
- **Remotion e HyperFrames resolvem o mesmo problema.** Só existe a ponte `remotion-to-hyperframes`; não há o inverso. Manter os dois é escolha deliberada, não redundância: HyperFrames para o fluxo com skills, Remotion para composição React à mão. Cada peça declara o seu no `BRIEFING.md` (regra completa no `FRAMEWORK.md`).
- **Licença do Remotion não é MIT.** `LICENSE.md` do pacote: grátis para indivíduo, organização sem fins lucrativos, empresa **de até 3 funcionários** e avaliação; acima disso exige Company License paga (remotion.pro). **Confirmar o enquadramento da agência e do cliente antes de usar em produção.**
- **Sem rede de fontes no render.** Google Fonts está fora da allowlist, então a fonte declarada em `marca.ts` cai para a sans do sistema no Remotion headless. Para usar a fonte da marca, embutir o arquivo em `assets/fonts/` como asset local.
- **A URL do ffmpeg estático quebrou em 18/set/2026, e a diferença é de uma palavra.** `releases/download/latest/…` é a tag rolante do BtbN e **serve o arquivo**; `releases/latest/download/…` parece equivalente e **não é**: resolve para a autobuild do dia, cujos assets têm outro nome, e devolve 404. **Os 403 do apt são ruído conhecido e esperado**; o que mata é o fallback não entregar o binário. O `setup.sh` usa as duas URLs em ordem e **confere a assinatura XZ (`fd377a585a00`) antes de extrair**; sem isso um 404 chega como corpo de texto e o `tar` reclama de "not a tar archive", escondendo a causa.
- **O setup do environment falha por caminho relativo, não por gatilho instável.** O campo de setup script do environment com `bash scripts/setup.sh` roda com o diretório de trabalho no **pai** do repo, dá `No such file or directory`, **exit 127**, e a sessão nasce sem `/workspace` e sem skills (foi o estado desta sessão em 18/set/2026: ffmpeg ausente, `/workspace` vazio). Corrigir com caminho absoluto, `bash /home/user/drvictorferigato-conteudo/scripts/setup.sh`, ou com a versão à prova de diretório:
  ```bash
  for p in ./scripts/setup.sh ./*/scripts/setup.sh; do [ -f "$p" ] && exec bash "$p"; done; p=$(find /home /workspace /repo /app /src -maxdepth 4 -type f -path "*/scripts/setup.sh" 2>/dev/null | head -1); [ -n "$p" ] && exec bash "$p"; echo "setup.sh nao encontrado no repo"; exit 1
  ```
  O script deriva o próprio `REPO_ROOT` do `BASH_SOURCE`, então roda de qualquer diretório desde que seja invocado pelo caminho certo. Enquanto o campo não for corrigido, rodar à mão em sessão nova. **Merge não conserta a sessão**: o script só roda no boot de container novo.
- **O patch `video-use-is-portrait-source` foi aposentado (18/set/2026).** O upstream reescreveu `is_portrait_source` para ler também o `rotation` do side data. O `validate.sh` testa **comportamento** (retrato, paisagem e paisagem com matriz de rotação 90) em vez de procurar patch no código. O arquivo `patches/` foi removido deste repo.
- Legendas SEMPRE por último no filter chain; overlays via PIL em PNG sequence + qtrle (ou PNG estático com fade de alpha).
- Zoom animado com `zoompan`, não `crop` (crop não aceita `t` em w/h).
- Metricool MCP: sem delete (cancelar = update `draft:true`; update devolve id novo); mídia por URL pública (o Metricool copia para o CDN dele na hora).
- **Metricool, rascunho com data vencida não publica e não avisa.** Um post `draft:true` cuja data passa continua no calendário, aparece em `getScheduledPosts` como se estivesse agendado, e nunca dispara. Regra: **quem agenda tira do rascunho na mesma sessão e confirma com `getScheduledPosts`**; nunca deixar o flip de `draft` para uma sessão seguinte. Tirar do rascunho com a data no passado também não resolve, é preciso data nova.
- **Ler o índice de um ZIP gigante no Drive sem baixar o arquivo.** `drive.usercontent.google.com` aceita `Range`, então dá para pegar os últimos ~64 KB, achar o EOCD (`PK\x05\x06`) e, em arquivo >4 GB, o ZIP64 EOCD via locator `PK\x06\x07`, ler o central directory e listar tudo. Entradas `method=0` (stored), como nos exports do OneDrive com MP4, podem ser extraídas uma a uma por outro `Range` no offset do local header. Evita baixar 11 GB para pegar um vídeo de 90 MB. Script: `scripts/zip_index_remoto.py`.
- Mac: usar ffmpeg-full keg-only com PATH explícito. Linux/cloud: `apt` está bloqueado; o `setup.sh` instala o build estático do BtbN (gpl, com `subtitles`, `zscale`, `lut3d`, `zoompan` e `loudnorm`).
- Cloud, brutos do Drive: environment com network **Custom** e `drive.google.com` + `drive.usercontent.google.com` + `api.elevenlabs.io` liberados. Download direto de arquivo público, qualquer tamanho: `curl -L "https://drive.usercontent.google.com/download?id=<ID>&export=download&confirm=t"`. O conector MCP do Drive serve para busca e metadados; download por ele só até ~4 MB. Fallback para arquivo público pequeno: Kairogen `download_audio_from_url`.
- Cloud, mídia pública para o Metricool: commit temporário do render na branch (repo público, `raw.githubusercontent.com` passa para o servidor do Metricool, que é quem baixa), agendar e remover o arquivo em seguida. Exige `git add -f` (o `.gitignore` barra mídia) com autorização do usuário. **Por isso este repo deve ser público.**
- Cloud: env var de environment antigo pode conter um key ID (64 hex) em vez da chave; a chave real da ElevenLabs é `sk_...` de 51 caracteres.
- Trilhas/SFX: ElevenLabs `sound-generation` (`/v1/sound-generation`, máx ~22s, `duration_seconds` entre 0.5 e 30) gera beds e SFX ótimos; para trilha maior, gerar build+drop e costurar com `acrossfade`. Detecção de BPM/batidas: script próprio com numpy (fluxo de energia + autocorrelação), referência em `ana-conteudo/projects/teste-02-interlagos/edit/beats.py`.
- Testar escopo de chave da ElevenLabs sem gastar crédito: chamar o endpoint com parâmetro inválido. `401 missing_permissions` = escopo ausente; `400`/`404` de validação = escopo presente. Nem toda chave tem `user_read`; sem ele não dá para checar saldo antes de um lote grande.
- **Instagram exige login, mesmo liberado na rede.** Com `instagram.com`, `*.instagram.com` e `*.cdninstagram.com` na allowlist o host responde 200, mas ler perfil continua impossível de forma anônima: `/<perfil>/` devolve 302 para login, a API `web_profile_info` devolve 401, e o embed devolve casca vazia. **É política do Instagram, não é rede.** Para analisar feed (próprio ou de referência): prints/gravação de tela do usuário, o conector do Metricool (só contas conectadas à marca) ou a Graph API da Meta com token.
- **Allowlist do environment não cobre subdomínio.** A entrada é literal: `instagram.com` libera o apex mas não `www.instagram.com`; `www.googleapis.com` não libera `generativelanguage.googleapis.com`. O campo aceita `*`, então para um site inteiro o certo é `*.dominio.com` junto do apex.
- **Chromium não contorna a allowlist.** O headless em `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` usa o mesmo agent proxy e devolve `ERR_TUNNEL_CONNECTION_FAILED` no mesmo host que o `curl` recusa. Navegador só ajuda contra JS/SPA, nunca contra egresso bloqueado.
- **Skills do hyperframes sem rede**: `npx hyperframes skills update` falha quando `raw.githubusercontent.com` está fora da allowlist. Não é preciso liberar o domínio: o clone em `/workspace/heygen-com/hyperframes/skills/<nome>/SKILL.md` já traz as 20 skills, e o `setup.sh` as linka em `~/.claude/skills/` quando o `npx` falha.
- **Sessão filha para o Metricool (rota validada, com armadilhas)**: follow-up na mesma sessão via `create_trigger` com `persistent_session_id` + `fire_trigger` **sem `text`** (com `text` o disparo ignora a vinculação e cria sessão nova no environment do trigger, que morre no setup do estúdio). Sessão arquivada não aceita trigger: `unarchive_session` antes. O `post_turn_summary` comprime a resposta e **engole números/ids**; dado exato que precise voltar deve ser publicado pela filha num canal durável, não no resumo.

## Histórico de decisões

- **18/ago/2026**: marca confirmada no Metricool (`getBrandSettings`: "drvictorferigato", blog_id 6606429).
- **18/set/2026**: PR `claude/conserta-setup-ffmpeg` mergeado: ffmpeg estático com as duas URLs e checagem XZ, rota de rede pelo proxy, nenhum passo derruba o boot.
- **18/set/2026**: **cinto de ferramentas portado** do `profissioai-conteudo` (branch `claude/reels-automaticos-profissio-d0qk2d`, commit `f89e6f8`) seguindo o `PORTAR.md` de lá. Entrou: 6 scripts genéricos (varredura de marca: zero referências), `validate.sh` novo (testa comportamento, rede e render do Remotion), `remotion/` com `marca.ts` em placeholder declarado e `Root.tsx` sem texto de outra marca, `colour-science` no passo 5 do `setup.sh` (o `gera_lut_slog2.py` exige e o guia prometia sem instalar), patch aposentado removido, gotchas genéricos aqui e regra de framework no `FRAMEWORK.md`. **Não** entrou: `CLAUDE.md`/`FRAMEWORK.md` de lá, `projects/`, `design-system/`, `assets/sfx/` (mídia de outra marca). `setup.sh` e `validate.sh` rodados nesta sessão e **100% verdes** (ffmpeg N-126636 estático, Remotion 4.0.526 renderizou 1 frame com o `headless_shell`, `is_portrait_source` acerta os 3 casos). Pendências que dependem do environment, não do repo: chaves `OPENAI_API_KEY`/`GEMINI_API_KEY` e liberação de `api.openai.com`, `generativelanguage.googleapis.com` e `www.googleapis.com`.
